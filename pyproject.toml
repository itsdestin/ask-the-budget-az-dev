[project]
name = "ask-the-budget-az-phase-0"
version = "0.1.0"
description = "Ask the Budget AZ — Phase 0 extractor bake-off + ingest scripts"
requires-python = ">=3.12"
dependencies = [
    "pyyaml>=6.0.3",
    "mineru[pipeline]>=3.1.6",
]

[dependency-groups]
dev = [
    "pypdf>=6.10.2",
    "pytest>=9.0.3",
]
